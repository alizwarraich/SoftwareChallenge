import {mapStateToProps} from './SearchContainer';

describe('(Container) BodyContainer', () => {
  describe('mapStateToProps', () => {
    it('returns state with `tweets` props', () => {});
  });
});