const layout = {
  margin: '100px auto',
  width: '400px',
  border: '1px solid #8f8f8f'
};

const formLayout = {
  width: '100%',
  padding: '15px 15px',
};

const resultLayout = {
  width: '100%',
  padding: '15px 15px',
};

const inputLayout = {
  width: '100%',
  margin: 'auto'
};

const textInput = {
  width: '100%',
};

const submitInput = {
  width: '100%',
};

const card = {
  margin: '5px 0 0 0',
  padding: '15px 15px',
  textAlign: 'left',
  minHeight: '40px',
};

export default {
  layout,
  formLayout,
  resultLayout,
  inputLayout,
  textInput,
  submitInput,
  card,
};